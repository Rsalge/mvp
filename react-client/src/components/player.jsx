import React from 'react';
//Each list item will be a players scorecard for a specific turn
const Player = (props) => (
  <input
    type="text"
    value={this.state.players[0]}
    onChange={this.updatePlayerName.bind(this, 0)}
  />
)

export default Player;
